apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "telemetry-chart.fullname" . }}-test-connection"
  labels:
    {{- include "telemetry-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "telemetry-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
